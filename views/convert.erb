<h1>Convert <%= @original_currency %></h1>
<p><a href="/">Go back</a>.</p>
<ul>
  <% @parsed_data["currencies"].each do |code, name| %>
    <% next if code == @original_currency %>
    <li>
      <a href="/<%= @original_currency %>/<%= code %>">
        Convert 1 <%= @original_currency %> to <%= code %>...
      </a>
    </li>
  <% end %>
</ul>
